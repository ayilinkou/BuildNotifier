# BuildNotifier

A simple Visual Studio extension which sends a notification when the build fails or succeeds.

# Installation

Download the latest release of the extension and run `BuildNotifier.vsix`. Alternatively, you can build the extension yourself from source and run `bin/Release/BuildNotifier.vsix`. You may need to restart Visual Studio for the extension to take effect.
